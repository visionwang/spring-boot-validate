spring:
  application:
    name: validate
  profiles.active: dev
  cloud.config.failFast: false

---
spring:
  profiles: dev
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}

---
spring:
  profiles: svt
  cloud:
    config:
      uri: http://yuuyoo-config:8888
---
spring:
  profiles: pro
  cloud:
    config:
      uri: http://yuuyoo-config:8888

